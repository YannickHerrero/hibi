---
"hibi-client": minor
---

Initial public release. Surface includes cards CRUD, reviews due/submit, and stats (heatmap, retention, daily counts). Runtime Zod validation against `@hibi/types`. ESM + CJS + types via tsup.
